// Copy to Config/ReleaseSecrets.xcconfig for local Release archives only.
// Do not commit the copy.
//
// This is useful for an internal TestFlight build from your machine, but it
// embeds any included API key in the app binary. Prefer Production.xcconfig with
// a server-side Anthropic proxy for anything distributed beyond internal testing.

#include "Secrets.xcconfig"
